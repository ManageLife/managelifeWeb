// Specifies paths for Link throughout the App
//     Helpful with nested routers

const links = {
   visitor: ['/', '/partnerships', '/about'],
   member: ['/feed', '/properties', '/inventory'],
}

export { links }
